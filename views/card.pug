extends layout.pug

block content
	section#content
		h2= text
		if hint
			p
				i Hint: #{hint}
		a(href=`${id}?side=${sideToShow}`)
			button Show #{sideToShowDisplay}
		br
		a(href='/cards')
			button Next Card